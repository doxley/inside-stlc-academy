-- ============================================================
-- Lesson enhancements for "api-testing-masterclass" Module 2.
-- Generated by scripts/content/build-module.mjs — do not hand-edit;
-- edit scripts/content/api-module2.mjs and regenerate. Run AFTER
-- lesson-enhancements-schema.sql and the lesson seeds. Idempotent.
-- ============================================================

update public.lessons l set enhancements = $ench${"industryStory":"A team shipped a `GET /accounts?ownerId=` endpoint where a missing `ownerId` was ignored rather than rejected — so the filter silently returned every account. The API tests only checked for `200 OK`, so the data-leak sailed through. One assertion that every row matched the requested owner would have caught it.","visualAid":{"type":"comparison","title":"Collection vs single resource","headers":["Request","Returns","Miss behaviour"],"rows":[["GET /users","JSON array (collection)","empty array + 200"],["GET /users/1","JSON object (one item)","404 Not Found"],["GET /users?role=admin","filtered array","empty array + 200"],["GET /users?page=2","a page of the array","empty array + 200"]]},"davidTip":"When I test a read endpoint I always ask: what happens at the edges? Page zero, page beyond the data, an unknown filter value, a deleted id. The happy path is easy — the edges are where the real bugs hide.","badGood":{"label":"asserting a filtered GET","bad":"The test only checks `status == 200` for `GET /users?role=admin`. It passes even when the filter is ignored and every user is returned.","good":"The test checks `status == 200` and asserts that **every** item in the array has `role == \"admin\"`, plus a sensible result count."},"miniChallenge":"Send `GET https://jsonplaceholder.typicode.com/posts?userId=1` and confirm every post in the response has `userId` of `1`.","modelAnswer":"## Example\nStatus `200 OK`. The body is a JSON array of ~10 posts, and every object has `\"userId\": 1`. I verified by scanning the `userId` field on each item rather than trusting the status code alone."}$ench$::jsonb
from public.modules m join public.courses c on c.id = m.course_id
where l.module_id = m.id and c.slug = 'api-testing-masterclass'
  and m.module_number = 2 and l.lesson_number = 1;

update public.lessons l set enhancements = $ench${"industryStory":"A checkout bug once double-charged customers because a slow network made the app resend a `POST /orders` — and the API happily created two orders. `POST` is not idempotent, so the fix was an idempotency key on the request. The tester who reproduced it simply sent the same `POST` twice and watched two orders appear.","visualAid":{"type":"timeline","title":"Creating a resource with POST","steps":[{"label":"Build","detail":"method POST, Content-Type: application/json, JSON body"},{"label":"Send","detail":"body travels to the collection endpoint"},{"label":"Create","detail":"server stores it and assigns an id"},{"label":"Respond","detail":"201 Created + Location header + echoed body"},{"label":"Verify","detail":"GET the new id to confirm it was stored"}]},"davidTip":"The single most useful habit with `POST` is the follow-up `GET`. A `201` is a promise; the `GET` is the proof. I have caught endpoints that returned `201` but never actually persisted the record.","badGood":{"label":"verifying a POST","bad":"The test sends the `POST` and asserts `status == 200`. It never inspects the body or confirms anything was created.","good":"The test asserts `status == 201`, that the response contains a new `id`, that the echoed fields match the input, then `GET`s that `id` and confirms it persisted."},"miniChallenge":"`POST` a JSON body to `https://reqres.in/api/users`, note the returned `id`, and confirm the response echoes the fields you sent.","modelAnswer":"## Example\nI sent `{\"name\":\"Grace\",\"job\":\"tester\"}` to `POST /api/users` with `Content-Type: application/json`. Response: `201 Created`, body included `\"id\":\"631\"` and `\"createdAt\"`, and `name`/`job` matched exactly. I confirmed the `201` and the echoed fields rather than trusting a bare `200`."}$ench$::jsonb
from public.modules m join public.courses c on c.id = m.course_id
where l.module_id = m.id and c.slug = 'api-testing-masterclass'
  and m.module_number = 2 and l.lesson_number = 2;

update public.lessons l set enhancements = $ench${"industryStory":"A profile-edit form used `PUT` but only sent the fields on screen. Users who edited their display name silently lost their saved address, because `PUT` replaced the whole record with a partial body. The fix was a one-word change to `PATCH` — and a test that re-read the profile and diffed every field.","visualAid":{"type":"comparison","title":"PUT vs PATCH","headers":["Aspect","PUT","PATCH"],"rows":[["Intent","Replace whole resource","Update part of it"],["Body","Full representation","Only changed fields"],["Omitted fields","Wiped / defaulted","Left unchanged"],["Idempotent","Yes","Usually yes"],["Best for","Full overwrite","Single-field edits"]]},"davidTip":"My rule: if you only mean to change one field, use `PATCH`. And whichever you use, re-`GET` the resource and compare the whole object. I have seen more data lost to a careless `PUT` than to almost any other single mistake.","badGood":{"label":"editing one field","bad":"Client sends `PUT /users/1` with just `{\"name\":\"New\"}`. The server replaces the record and the user's email and phone are wiped.","good":"Client sends `PATCH /users/1` with `{\"name\":\"New\"}`. Only the name changes; a re-`GET` confirms email and phone are untouched."},"miniChallenge":"`GET` a resource, `PATCH` a single field, then re-`GET` and confirm that one field changed and every other field is identical.","modelAnswer":"## Example\nI `GET` `/users/2` (name \"Janet\", email present). I `PATCH` with `{\"name\":\"Janet R\"}` → `200 OK`. Re-`GET` shows `name` is now \"Janet R\" and the `email` is byte-for-byte the same. Because it was a `PATCH`, the untouched fields survived — a `PUT` with the same body could have wiped them."}$ench$::jsonb
from public.modules m join public.courses c on c.id = m.course_id
where l.module_id = m.id and c.slug = 'api-testing-masterclass'
  and m.module_number = 2 and l.lesson_number = 3;

update public.lessons l set enhancements = $ench${"industryStory":"An engineer flagged a \"bug\": the second `DELETE` of a record returned `404`. We talked through idempotency and realised it was correct — the record was gone and stayed gone. The real lesson was that the *contract* hadn't documented the second-delete behaviour, so nobody knew what to expect. We fixed the docs, not the code.","visualAid":{"type":"timeline","title":"Deleting the same resource twice","steps":[{"label":"DELETE #1","detail":"resource removed → 204 No Content"},{"label":"GET","detail":"resource is gone → 404 Not Found"},{"label":"DELETE #2","detail":"nothing left to remove → 404 (or 204 by design)"},{"label":"State","detail":"resource is gone and stays gone — idempotent"}]},"davidTip":"When I test a `DELETE` I write down the expected second-delete behaviour *before* I run it, then compare. If the API and the docs disagree, that mismatch is the bug — not the status code itself.","badGood":{"label":"testing a delete","bad":"The test sends one `DELETE`, asserts `204`, and stops — never confirming the resource is actually gone.","good":"The test asserts `204`, then `GET`s the id and asserts `404`, then re-sends the `DELETE` and checks it matches the documented idempotent behaviour."},"miniChallenge":"`DELETE` a resource, record the status, then `DELETE` the same id again and note the second response. State whether the overall behaviour is idempotent and why.","modelAnswer":"## Example\nFirst `DELETE /api/users/2` → `204 No Content`. Second `DELETE /api/users/2` → `404 Not Found`. The behaviour is idempotent because the resource is gone after the first call and remains gone; the differing status code just reflects \"already absent\", while the end state never changes."}$ench$::jsonb
from public.modules m join public.courses c on c.id = m.course_id
where l.module_id = m.id and c.slug = 'api-testing-masterclass'
  and m.module_number = 2 and l.lesson_number = 4;

update public.lessons l set enhancements = $ench${"industryStory":"A payments API returned `500` whenever an amount was sent as a string instead of a number. It had been dismissed as \"just an edge case\" for months. When we reframed it — a `5xx` on bad input is a server bug, not a test quirk — it was fixed in a day, and it turned out a real client integration had been hitting it in production.","visualAid":{"type":"comparison","title":"The status code families","headers":["Class","Meaning","Whose \"fault\"","Common examples"],"rows":[["2xx","Success","Nobody","200, 201, 204"],["3xx","Redirection","Nobody","301, 304"],["4xx","Client error","The caller","400, 401, 403, 404, 429"],["5xx","Server error","The server","500, 502, 503"]]},"davidTip":"The first number of a status code tells you where to look before you read a single line of the body. `4` means check your request. `5` means check their server — and probably raise a bug. That instinct saves hours of triage.","badGood":{"label":"asserting a status code","bad":"The test asserts `status >= 200 && status < 300`. It passes on `200` even when the contract required `201 Created`.","good":"The test asserts the exact code, e.g. `status == 201` for a create and `status == 404` for a missing id, so a wrong-but-successful code is caught."},"miniChallenge":"Deliberately trigger a `200`, a `404` and a `400` (or `405`) against a public API, and note exactly what caused each.","modelAnswer":"## Example\n`GET /posts/1` → `200` (valid read). `GET /posts/99999` → `404` (unknown id). `POST /posts` with a broken body / wrong method → `400` or `405`. Each status matched the cause, and none returned `500`, which told me the endpoint validated input rather than crashing on it."}$ench$::jsonb
from public.modules m join public.courses c on c.id = m.course_id
where l.module_id = m.id and c.slug = 'api-testing-masterclass'
  and m.module_number = 2 and l.lesson_number = 5;

update public.lessons l set enhancements = $ench${"industryStory":"A mobile app kept getting empty responses from an endpoint that worked fine in the browser. Hours of debugging later, the cause was one header: the app sent `Accept: application/xml` while the API only produced JSON, so it returned `406 Not Acceptable` with no body. One glance at the request headers would have solved it in a minute.","visualAid":{"type":"comparison","title":"Request headers vs query parameters","headers":["Concern","Where it lives","Example","Controls"],"rows":[["Body format sent","Content-Type header","application/json","How the server reads your body"],["Format wanted back","Accept header","application/json","What the server returns"],["Credentials","Authorization header","Bearer <token>","Who you are"],["Filter / sort / page","Query string","?role=admin&page=2","Which data comes back"]]},"davidTip":"First person, honest habit: when an API \"misbehaves\", I open the raw request and read every header before I touch anything else. Nine times out of ten the answer is a wrong `Content-Type`, a missing token, or a response format I didn't expect. Headers are the cheapest bug fix in testing.","badGood":{"label":"sending a JSON body","bad":"Body is set to `{\"name\":\"Ada\"}` but no `Content-Type` header is set. The server can't tell it's JSON and returns `415` or ignores the body.","good":"The request sets `Content-Type: application/json` alongside the JSON body, and asserts the response `Content-Type` is `application/json` too."},"miniChallenge":"Send `GET /comments?postId=1`, confirm the response `Content-Type` is `application/json`, then add a second query parameter and verify the results change.","modelAnswer":"## Example\n`GET https://jsonplaceholder.typicode.com/comments?postId=1` → `200 OK`, response header `Content-Type: application/json`, body is a JSON array where every item has `\"postId\": 1`. Adding `&id=1` narrowed the result to a single comment, confirming the query parameters were honoured — and I saved both requests into my collection.","managersReview":{"intro":"If I reviewed your Module 2 work as a lead before signing it off, here's what I'd look at:","strengths":["You assert exact status codes, not just \"2xx-ish\"","You re-`GET` after `POST`/`PUT`/`PATCH`/`DELETE` to prove the state","You inspect headers and query parameters, not only the body"],"improvements":["Add negative cases: bad input, missing auth, wrong method","Document expected second-`DELETE` behaviour before running it","Assert response `Content-Type`, not just the status code"],"gaps":["No test that a `5xx` never appears for bad input","Filters checked only by status, not by verifying returned items","Auth headers not covered (`401` vs `403`)"]},"portfolioBuilder":"Extend your Postman collection \"API Masterclass — <your name>\" with one request per method (`GET`, `POST`, `PUT`, `PATCH`, `DELETE`), each with an exact status-code check and a header assertion. This method-and-status coverage becomes a strong section of your Module 10 portfolio project.","resourcePreview":{"name":"HTTP Status & Method Reference Card","purpose":"One-page lookup for methods, safe/idempotent properties, and every common status code.","whenToUse":"Keep it beside Postman whenever you write assertions.","formats":["PDF"]}}$ench$::jsonb
from public.modules m join public.courses c on c.id = m.course_id
where l.module_id = m.id and c.slug = 'api-testing-masterclass'
  and m.module_number = 2 and l.lesson_number = 6;

