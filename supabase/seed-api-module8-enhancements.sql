-- ============================================================
-- Lesson enhancements for "api-testing-masterclass" Module 8.
-- Generated by scripts/content/build-module.mjs — do not hand-edit;
-- edit scripts/content/api-module8.mjs and regenerate. Run AFTER
-- lesson-enhancements-schema.sql and the lesson seeds. Idempotent.
-- ============================================================

update public.lessons l set enhancements = $ench${"industryStory":"A team had a gorgeous Postman collection that only ever ran when one tester manually clicked \"Run\". When she was on leave, nothing got tested. The fix took ten minutes: `npm i -g newman` and the same collection ran on every commit. Automation isn't a new tool — it's the same tool without the human clicking.","visualAid":{"type":"comparison","title":"Postman app vs Newman","headers":["Aspect","Postman app","Newman"],"rows":[["Interface","Graphical (click Run)","Command line"],["Best for","Building & exploring","Automation & CI"],["Runs the same collection","Yes","Yes"],["Needs a human","Yes","No"],["Fits in a pipeline","No","Yes"]]},"davidTip":"The first time I got a Postman collection running under Newman, it felt like magic — but it's just the engine without the dashboard. Build in the app, run with Newman. That split is the whole mental model.","miniChallenge":"Run `npm i -g newman` then `newman --version`. If it errors with \"command not found\", what is the most likely cause?","modelAnswer":"## Example\nMost likely Node.js/npm isn't installed, or the global npm bin folder isn't on your PATH. Installing Node and reopening the terminal usually fixes it. A healthy result prints a version like `6.2.1`."}$ench$::jsonb
from public.modules m join public.courses c on c.id = m.course_id
where l.module_id = m.id and c.slug = 'api-testing-masterclass'
  and m.module_number = 8 and l.lesson_number = 1;

update public.lessons l set enhancements = $ench${"industryStory":"A tester swore her API \"worked for one user but broke for others\". Rather than test names by hand, we dropped 50 rows into a CSV and ran `newman run collection.json -e env.json -d users.csv`. Three rows failed instantly — a name with an apostrophe broke the payload. Data files turned a vague hunch into a precise, repeatable bug report.","visualAid":{"type":"comparison","title":"Common Newman flags","headers":["Flag","Purpose","Example"],"rows":[["(none)","Run a collection","newman run collection.json"],["-e","Attach an environment","-e env.json"],["-d","Data file (iterate per row)","-d users.csv"],["-n","Number of iterations","-n 5"],["--folder","Run one folder only","--folder 'Smoke'"]]},"davidTip":"I keep the exported collection and environment in the repo and treat `newman run` as the single source of truth. If someone says \"it passed on my machine\", I ask them to run the Newman command — no more, no less.","badGood":{"label":"running a collection","bad":"`newman run collection.json` — no environment, so `{{baseUrl}}` is blank and every request fails with a confusing 404.","good":"`newman run collection.json -e env.json` — the environment resolves `{{baseUrl}}` and `{{token}}`, so the run reproduces exactly what you saw in the app."},"miniChallenge":"You have a `prices.csv` with 10 rows and want to test the same endpoint for each. Write the full Newman command including an environment.","modelAnswer":"## Example\n`newman run collection.json -e env.json -d prices.csv`. Newman runs the collection ten times, once per CSV row, substituting each row's columns as variables."}$ench$::jsonb
from public.modules m join public.courses c on c.id = m.course_id
where l.module_id = m.id and c.slug = 'api-testing-masterclass'
  and m.module_number = 8 and l.lesson_number = 2;

update public.lessons l set enhancements = $ench${"industryStory":"A tester kept pasting 400 lines of green terminal text into Slack after every run — nobody read it. She switched to `-r htmlextra`, dropped the HTML report link into the channel, and suddenly the whole team was clicking through failures themselves. Same data, radically better reach. Presentation is part of the job.","visualAid":{"type":"comparison","title":"Choosing a reporter","headers":["Reporter","Output","Best audience"],"rows":[["cli","Terminal text","You, running live"],["json","JSON file","Other tools / scripts"],["junit","XML file","CI test dashboards"],["htmlextra","Rich HTML page","Managers & stakeholders"]]},"davidTip":"I always run `cli,htmlextra` together: cli so I see it live, htmlextra so I have something to share afterwards. Costs nothing extra and saves the \"can you send me the results?\" round-trip.","badGood":{"label":"sharing results","bad":"Pasting hundreds of lines of raw terminal output into a chat and asking people to \"scroll to the red bit\".","good":"`newman run collection.json -e env.json -r htmlextra` and sharing the colour-coded HTML report — failures are front and centre."},"miniChallenge":"Write one Newman command that shows a live terminal summary *and* writes an HTML report, using an environment file.","modelAnswer":"## Example\n`newman run collection.json -e env.json -r cli,htmlextra`. The `cli` reporter prints live; `htmlextra` writes a shareable HTML file (under `newman/` by default).","resourcePreview":{"name":"Sample Newman HTML Report","purpose":"An example htmlextra report to show what \"good\" looks like.","whenToUse":"Open it before your first run so you know what to expect.","formats":["HTML"]}}$ench$::jsonb
from public.modules m join public.courses c on c.id = m.course_id
where l.module_id = m.id and c.slug = 'api-testing-masterclass'
  and m.module_number = 8 and l.lesson_number = 3;

update public.lessons l set enhancements = $ench${"industryStory":"A team proudly showed me their \"green\" CI pipeline running Newman on every commit. But their collection only sent requests — no assertions. Every run exited 0, so the build was green while the API returned garbage. We added real `pm.test` assertions, and the next broken deploy went red in seconds. Green means nothing without assertions behind it.","visualAid":{"type":"timeline","title":"Newman in a CI pipeline","steps":[{"label":"Push","detail":"developer commits code"},{"label":"Checkout","detail":"CI pulls the repo (collection + env included)"},{"label":"Install","detail":"npm i -g newman"},{"label":"Run","detail":"newman run collection.json -e env.json"},{"label":"Verdict","detail":"exit 0 → green · non-zero → red"}]},"davidTip":"I treat the exit code as a promise: \"if this is non-zero, we do not ship\". Everything else — reports, dashboards — is decoration on top of that one guarantee. Get the assertions right first.","badGood":{"label":"a CI Newman step","bad":"A collection full of requests but no `pm.test` assertions. Newman always exits 0, so CI is permanently green and catches nothing.","good":"Each request asserts status and key fields, so a real failure exits non-zero and turns the build red — automation that actually protects you."},"miniChallenge":"Write the two pipeline lines that (a) install Newman and (b) run a collection with its environment.","modelAnswer":"## Example\n```yaml\n- run: npm i -g newman\n- run: newman run collection.json -e env.json\n```\nLine (a) installs the runner; line (b) runs the collection. A non-zero exit from line (b) fails the job."}$ench$::jsonb
from public.modules m join public.courses c on c.id = m.course_id
where l.module_id = m.id and c.slug = 'api-testing-masterclass'
  and m.module_number = 8 and l.lesson_number = 4;

update public.lessons l set enhancements = $ench${"industryStory":"A payments API we relied on had its TLS certificate expire over a bank holiday weekend. Our code hadn't changed, so CI never ran — but an hourly scheduled Newman smoke test failed at 06:00 Saturday and paged the on-call engineer. We had it flagged with the vendor before customers were awake. That single cron line saved a Monday of angry tickets.","visualAid":{"type":"comparison","title":"CI runs vs scheduled runs","headers":["Aspect","CI run","Scheduled run"],"rows":[["Trigger","Code change / commit","Time (cron)"],["Catches","Regressions from new code","Silent / external failures"],["Environment","Usually test/staging","Often staging or production"],["Doubles as","Quality gate","Lightweight monitoring"]]},"davidTip":"My rule: a tiny smoke collection runs often, the full suite runs on commits and overnight. Frequent + small beats occasional + huge. And I only ever alert on red — an inbox of green emails trains people to ignore all of them.","badGood":{"label":"a monitoring schedule","bad":"The full 300-request regression suite runs every 5 minutes and emails the team on every run, pass or fail. Costs pile up and everyone mutes the alerts.","good":"A 3-request smoke collection runs hourly against staging, alerting only on failure — cheap, quiet, and impossible to ignore when it matters."},"miniChallenge":"Write a cron expression for \"every 15 minutes\" and pair it with a Newman command that runs a smoke collection against staging with an HTML report.","modelAnswer":"## Example\nCron: `*/15 * * * *`. Command: `newman run smoke.json -e staging.json -r cli,htmlextra`. Together they run the smoke collection every 15 minutes and leave a shareable report each time.","managersReview":{"intro":"If I reviewed your automation work at the end of this module, here's what I'd be looking for:","strengths":["A collection that runs cleanly under Newman","Real assertions so exit codes are meaningful","An htmlextra report you can share","Tests wired into CI on every commit"],"improvements":["Add a scheduled smoke run against staging","Alert on failure only, not on every run","Keep collection + environment versioned in the repo"],"gaps":["A pipeline that goes green with no assertions behind it","Committing secrets into env.json instead of using CI secrets","Scheduling the full suite too aggressively"]},"portfolioBuilder":"Add an \"API automation\" artefact to your portfolio: your exported collection and environment, the Newman command you run them with, a sample htmlextra report, and the CI/schedule YAML. Together these prove you can take an API from manual testing all the way to automated, monitored coverage.","resourcePreview":{"name":"Newman Automation Starter Kit","purpose":"A ready-to-adapt CI workflow plus cron schedule for running collections with htmlextra.","whenToUse":"Copy it into your own repo when you set up your first automated pipeline.","formats":["YAML","Markdown"]}}$ench$::jsonb
from public.modules m join public.courses c on c.id = m.course_id
where l.module_id = m.id and c.slug = 'api-testing-masterclass'
  and m.module_number = 8 and l.lesson_number = 5;

