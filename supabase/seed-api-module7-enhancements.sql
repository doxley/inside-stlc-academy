-- ============================================================
-- Lesson enhancements for "api-testing-masterclass" Module 7.
-- Generated by scripts/content/build-module.mjs — do not hand-edit;
-- edit scripts/content/api-module7.mjs and regenerate. Run AFTER
-- lesson-enhancements-schema.sql and the lesson seeds. Idempotent.
-- ============================================================

update public.lessons l set enhancements = $ench${"industryStory":"A team migrated a mobile app from REST to GraphQL to cut round trips. Their old API tests were copied across almost unchanged — still asserting `status == 200`. They stayed green for weeks while a broken resolver quietly returned `data: null` with an `errors` array. Nobody had checked the body. The GraphQL model punished them for testing it like REST.","visualAid":{"type":"comparison","title":"GraphQL vs REST","headers":["Dimension","REST","GraphQL"],"rows":[["Endpoints","Many (per resource)","One (POST /graphql)"],["Response shape","Server decides","Client asks for it"],["Over/under-fetching","Common","Avoided"],["Errors signalled by","HTTP status","errors array (often 200)"],["Contract","Docs / OpenAPI","Typed schema"]]},"davidTip":"The hardest habit to unlearn moving from REST to GraphQL is trusting the status code. In REST a 200 means \"good\". In GraphQL a 200 means \"the server received your request\" — nothing more. Read the body every single time.","miniChallenge":"Write, on paper, a GraphQL query that fetches the `name` and `capital` of the country with code `\"JP\"` from `countries.trevorblades.com`.","modelAnswer":"## Example\n```graphql\nquery {\n  country(code: \"JP\") {\n    name\n    capital\n  }\n}\n```\nSent as `POST /graphql`, this returns `200 OK` with `data.country.name` = \"Japan\" and `data.country.capital` = \"Tokyo\".","badGood":{"label":"judging a GraphQL response","bad":"The test asserts `status == 200` and calls it a pass — even though the body is `{ \"errors\": [...], \"data\": null }`.","good":"The test asserts `status == 200`, that the `errors` field is absent, and that `data.country.name` equals the expected value."}}$ench$::jsonb
from public.modules m join public.courses c on c.id = m.course_id
where l.module_id = m.id and c.slug = 'api-testing-masterclass'
  and m.module_number = 7 and l.lesson_number = 1;

update public.lessons l set enhancements = $ench${"industryStory":"A tester I worked with built a lovely GraphQL query suite that checked `data` values precisely — but never checked for `errors`. A schema change made one nested field start failing to resolve; the API returned that field as `null` inside `data` with an explanatory `errors` entry. The suite stayed green because it only looked at the fields it expected. One `errors`-must-be-absent assertion would have caught it instantly.","visualAid":{"type":"timeline","title":"Asserting a GraphQL query","steps":[{"label":"Send","detail":"POST /graphql with query + variables"},{"label":"Status","detail":"expect 200 (necessary, not sufficient)"},{"label":"Errors","detail":"assert the errors field is absent"},{"label":"Data","detail":"assert shape and values of data"},{"label":"Fields","detail":"confirm only requested fields returned"}]},"davidTip":"Use named queries and variables from day one, even in exploratory testing. `query GetCountry($code: ID!)` reads far better in a failing test report than a string with the value baked in — and it lets you reuse the same query across data sets.","miniChallenge":"Write a GraphQL query, using a `$code` variable, that fetches the `name` and `currency` of a country. Show the query and the accompanying `variables` JSON.","modelAnswer":"## Example\n```graphql\nquery GetCountry($code: ID!) {\n  country(code: $code) {\n    name\n    currency\n  }\n}\n```\nVariables: `{ \"code\": \"GB\" }`. Response: `data.country.name` = \"United Kingdom\", `data.country.currency` = \"GBP\", and no `errors` field.","badGood":{"label":"a GraphQL query test","bad":"`expect(res.status).toBe(200)` and nothing else — passes even when `data` is null and `errors` is populated.","good":"`expect(res.status).toBe(200); expect(res.body.errors).toBeUndefined(); expect(res.body.data.country.name).toBe('United Kingdom');`"}}$ench$::jsonb
from public.modules m join public.courses c on c.id = m.course_id
where l.module_id = m.id and c.slug = 'api-testing-masterclass'
  and m.module_number = 7 and l.lesson_number = 2;

update public.lessons l set enhancements = $ench${"industryStory":"A checkout team shipped a GraphQL mutation that silently ignored an invalid discount code — it returned `200` with a normal-looking payload and an `errors` entry nobody read. Orders went through at full price while the \"code applied\" message showed on screen. Their mutation tests only checked the status and the happy-path fields. The fix was one rule: every mutation test must also assert on the `errors` field, both when it should be absent and when it should be present.","visualAid":{"type":"comparison","title":"GraphQL mutation vs REST write","headers":["Concern","REST","GraphQL mutation"],"rows":[["Verb","POST / PUT / DELETE","mutation keyword"],["Endpoint","Per-resource URL","POST /graphql"],["Success signal","HTTP status (201, etc.)","errors absent + payload"],["Validation error","Usually 4xx","Usually 200 + errors"],["What to assert","Status + body","errors + returned payload"]]},"davidTip":"I always ask a mutation to return the thing it changed, then I assert on that payload rather than trusting a \"success\" flag. And I never test a mutation without also confirming the change persisted with a follow-up query — mutations that \"succeed\" but don't save are a classic silent bug.","miniChallenge":"Write a GraphQL mutation, using variables, that creates a record and returns its `id` and one other field. List the two assertions you'd make on the response.","modelAnswer":"## Example\n```graphql\nmutation AddUser($name: String!) {\n  insert_users(objects: { name: $name }) {\n    returning { id name }\n  }\n}\n```\nAssertions: (1) `errors` is absent and status is 200; (2) `data.insert_users.returning[0].name` equals the name sent and `id` is present.","badGood":{"label":"a mutation test","bad":"Sends the mutation, asserts `status == 200`, moves on — an invalid input that returns `errors` with `data: null` still passes.","good":"Happy path asserts `errors` absent and the returned payload; a second test sends invalid input and asserts `errors` is present with a meaningful message and no record was created."}}$ench$::jsonb
from public.modules m join public.courses c on c.id = m.course_id
where l.module_id = m.id and c.slug = 'api-testing-masterclass'
  and m.module_number = 7 and l.lesson_number = 3;

update public.lessons l set enhancements = $ench${"industryStory":"Two teams shared an API: a payments service (provider) and a checkout app (consumer). A provider developer renamed a field from `total` to `amount`, assuming nobody relied on the old name. It passed every unit test. It broke checkout in production the next morning. After that incident they adopted Pact: the consumer published a contract, the provider verified it on every build, and the same rename was caught in CI in under a minute — before it ever merged.","visualAid":{"type":"comparison","title":"Contract vs integration vs E2E testing","headers":["Aspect","Contract test","Integration / E2E"],"rows":[["Scope","Agreed shape between two sides","Real systems working together"],["Runs","In each pipeline, isolated","Late, both systems live"],["Speed","Seconds","Minutes+"],["Flakiness","Low","Higher"],["Catches","Shape/type breakages early","End-to-end behaviour"]]},"davidTip":"I think of a contract test as a promise with a witness. The consumer writes the promise; the provider's pipeline is the witness that checks it's still kept. If only one side runs it, you don't have contract testing — you have wishful thinking.","miniChallenge":"In two or three sentences, describe a consumer-driven contract for the countries API: the request the consumer sends and the exact fields and types it depends on.","modelAnswer":"## Example\nThe consumer sends `POST /graphql` with a query for `country(code: \"GB\") { name emoji }`. The contract states the response must contain `data.country.name` (string) and `data.country.emoji` (string). The provider verifies on each build that both fields are still returned with those types; renaming or removing either breaks verification.","badGood":{"label":"a contract test setup","bad":"The consumer team writes a contract and runs it against a stub. The provider never verifies it, so a field rename ships and breaks the consumer anyway.","good":"The consumer publishes the contract to a broker; the provider verifies it on every build and cannot merge a change that violates the agreed shape."}}$ench$::jsonb
from public.modules m join public.courses c on c.id = m.course_id
where l.module_id = m.id and c.slug = 'api-testing-masterclass'
  and m.module_number = 7 and l.lesson_number = 4;

update public.lessons l set enhancements = $ench${"industryStory":"A provider team removed a field they were \"certain\" no one used. It turned out a legacy reporting job queried it nightly; the job failed silently and finance discovered a fortnight of missing numbers. They introduced a schema-diff gate in CI plus consumer contracts, and adopted a strict rule: never remove, always deprecate first and wait for usage to hit zero. The next attempted removal was blocked in CI within seconds — the exact incident, prevented.","visualAid":{"type":"timeline","title":"Safely retiring a field","steps":[{"label":"Add","detail":"introduce the replacement field (additive, safe)"},{"label":"Deprecate","detail":"mark the old field @deprecated"},{"label":"Migrate","detail":"move consumers to the new field"},{"label":"Measure","detail":"watch old-field usage fall to zero"},{"label":"Remove","detail":"delete the old field once unused"}]},"davidTip":"The best breaking-change strategy is to not break things. Additive plus deprecation gets you almost everywhere. When you genuinely must break, do it behind a version and give consumers a migration window — and let CI, not a human reviewer, be the one that says no.","miniChallenge":"List three schema changes to the countries API — one safe, one breaking, one subtle breaker — and name the check that would catch each breaking one.","modelAnswer":"## Example\nSafe: add `Country.timezone` as an optional field. Breaking: rename `Country.emoji` to `Country.flag` — caught by both schema-diff (field removed) and Pact provider verification (contract expects `emoji`). Subtle breaker: change `country(code: String)` to `country(code: ID!)`, making the argument required and its type stricter — caught by schema-diff flagging a tightened, non-nullable argument; stage it by first accepting both, then deprecating the loose form.","badGood":{"label":"shipping a schema change","bad":"A developer renames a field and merges after a green unit-test run; consumers break in production the next day.","good":"The rename triggers a CI schema-diff and provider contract verification, both fail before merge; the team instead adds the new field, deprecates the old, and removes it only once usage is zero."},"managersReview":{"intro":"If I reviewed your GraphQL and contract-testing work as a lead, here is what I'd look for:","strengths":["You check the errors field on every query and mutation, never status alone","Mutations assert on the returned payload and test unhappy paths","You can explain consumer/provider contract testing and its value"],"improvements":["Turn your ad-hoc GraphQL checks into a reusable \"200 + no errors + data\" assertion helper","Add a schema-diff or contract gate to CI rather than relying on review","Document which changes are breaking so the whole team shares the rule"],"gaps":["Only testing happy paths and never asserting errors is present when it should be","A contract the provider never verifies — documentation, not a test","Removing fields without checking real consumer usage first"]},"portfolioBuilder":"Add a \"GraphQL & Contract Testing\" section to your portfolio: a small suite against `countries.trevorblades.com` (a query test and, if you can, a mutation test on a sandbox API), each asserting status, absence/presence of `errors`, and data shape. Include a one-page note describing a consumer-driven contract, a list of breaking vs safe changes, and how you'd gate them in CI.","resourcePreview":{"name":"GraphQL & Contract Testing Checklist","purpose":"A one-page reference for asserting GraphQL responses (status + errors + data) and classifying breaking vs safe schema changes.","whenToUse":"Keep it open whenever you test a GraphQL API or review a schema change.","formats":["PDF","Markdown"]}}$ench$::jsonb
from public.modules m join public.courses c on c.id = m.course_id
where l.module_id = m.id and c.slug = 'api-testing-masterclass'
  and m.module_number = 7 and l.lesson_number = 5;

