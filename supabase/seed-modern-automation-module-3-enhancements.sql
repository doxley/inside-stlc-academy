-- ============================================================
-- Lesson enhancements for "modern-test-automation-bootcamp" Module 3.
-- Generated by scripts/content/build-module.mjs — do not hand-edit;
-- edit scripts/content/modern-automation-module3.mjs and regenerate. Run AFTER
-- lesson-enhancements-schema.sql and the lesson seeds. Idempotent.
-- ============================================================

update public.lessons l set enhancements = $ench${"industryStory":"A team kept its regression suite on a shared network drive with no version control. A well-meaning engineer commented out a block of failing tests to get a green run before a demo, meaning to restore them afterwards, and forgot. Those tests stayed disabled for two release cycles until a production bug slipped through exactly where they would have caught it. Under Git the change would have been a visible, reviewable commit — someone would have asked \"why are we disabling these?\" before it ever merged.","visualAid":{"type":"comparison","title":"Test suite with and without version control","headers":["Concern","No version control","Under Git"],"rows":[["Recovering lost work","Hope someone has a copy","git revert / git checkout"],["Who changed this?","Guesswork","git blame / git log"],["Two people, one file","Overwrites and conflicts","Branches merged deliberately"],["Why was this changed?","Lost knowledge","Commit message on record"]]},"davidTip":"A useful habit: before changing a test suite you inherited, read its recent history first. The commits often explain a \"weird\" wait or an odd assertion far better than the code does — and stop you from re-introducing a bug someone already fixed.","miniChallenge":"In one sentence each, describe how history, collaboration and safety would each help on a suite you currently work on. Keep it concrete — name the real problem each one solves for you."}$ench$::jsonb
from public.modules m join public.courses c on c.id = m.course_id
where l.module_id = m.id and c.slug = 'modern-test-automation-bootcamp'
  and m.module_number = 3 and l.lesson_number = 1;

update public.lessons l set enhancements = $ench${"badGood":{"label":"a commit on a test suite","bad":"git add .\ngit commit -m \"tests\"\n\nEverything changed is dumped into one commit — a new test, an unrelated config tweak, and a stray `console.log` — under a message that explains nothing.","good":"git add tests/checkout.spec.ts\ngit commit -m \"Add checkout regression test for expired card\"\n\nOne focused change, staged deliberately after reviewing the diff, with a message a reviewer can understand at a glance."},"davidTip":"Get comfortable running `git diff --staged` as the last thing before every commit. It takes two seconds and it is the single best habit for keeping debug lines, secrets and half-finished experiments out of your team's history.","miniChallenge":"Take a commit message you would honestly have written in a hurry — \"fixed test\" or similar — and rewrite it as a clear imperative summary line under 60 characters that says what changed and, where useful, why.","visualAid":{"type":"flow","title":"How a change moves through Git","steps":[{"label":"Working directory","detail":"You edit login.spec.ts"},{"label":"git add","detail":"Stage the file into the index"},{"label":"Staging area","detail":"Review with git diff --staged"},{"label":"git commit","detail":"Snapshot recorded in history"}]}}$ench$::jsonb
from public.modules m join public.courses c on c.id = m.course_id
where l.module_id = m.id and c.slug = 'modern-test-automation-bootcamp'
  and m.module_number = 3 and l.lesson_number = 2;

update public.lessons l set enhancements = $ench${"industryStory":"Two engineers spent an afternoon untangling a merge that touched forty test files. The cause was not skill — it was time. One of them had been working on a \"big refactor\" branch for three weeks while the rest of the team merged into `main` daily. By the time the branch came back, it disagreed with `main` almost everywhere. The team's fix was a rule, not a tool: no branch lives longer than two days, and big refactors get broken into small merges. Conflicts became a non-event.","davidTip":"When a conflict looks scary, slow down and read both sides literally. The bit between `<<<<<<< HEAD` and `=======` is what is on your current branch; the bit below is what is coming in. Decide what the *correct final code* is — often it is a combination — and write that. Git is not trying to trick you; it is handing you a decision only a human can make.","miniChallenge":"Write down the branch name you would use for three real tasks: adding tests for a new search feature, fixing a flaky payment test, and upgrading a test dependency. Make each name something a teammate could understand without asking you.","visualAid":{"type":"flow","title":"Feature-branch lifecycle","steps":[{"label":"Branch from main","detail":"git switch -c fix-flaky-login"},{"label":"Commit work","detail":"Small, focused commits"},{"label":"Pull main in","detail":"Stay close to shared truth"},{"label":"Merge back","detail":"Resolve any conflict, then done"}]}}$ench$::jsonb
from public.modules m join public.courses c on c.id = m.course_id
where l.module_id = m.id and c.slug = 'modern-test-automation-bootcamp'
  and m.module_number = 3 and l.lesson_number = 3;

update public.lessons l set enhancements = $ench${"industryStory":"A team measured how long its pull requests sat waiting for review and found the biggest ones were also the slowest — a five-hundred-line PR could sit for days, then get approved in minutes with barely a comment. Nobody had really read it; it was too big to hold in mind. When the team adopted a soft limit of a couple of hundred changed lines per PR, review turned from a rubber stamp into a genuine conversation, and two real test defects were caught in the first week that a big-PR review would have skimmed straight past.","badGood":{"label":"a pull request review comment","bad":"\"Looks wrong to me.\"\n\nThe author has no idea what is wrong, where, or what to do about it. The comment starts an argument, not a fix.","good":"\"This test asserts the page title but the regression we're guarding against is an incorrect order total. Could we assert on the total as well, and wait on the confirmation element instead of the fixed 5s timeout?\"\n\nSpecific, tied to intent, and actionable — the author knows exactly what to change and why."},"davidTip":"When you review test code, ask one blunt question of every test: could this ever fail? If you cannot picture a realistic build where the assertion goes red, the test is not protecting anything — and a suite full of tests that cannot fail gives false confidence, which is worse than no confidence at all.","miniChallenge":"Draft a three-line PR description for a change that adds two tests and fixes one flaky one. Include what changed, why, and how you verified it — nothing more."}$ench$::jsonb
from public.modules m join public.courses c on c.id = m.course_id
where l.module_id = m.id and c.slug = 'modern-test-automation-bootcamp'
  and m.module_number = 3 and l.lesson_number = 4;

update public.lessons l set enhancements = $ench${"industryStory":"A team discovered an access token sitting in a committed `.env` file during a routine review. It had been there for months. Deleting the file was the easy part; the real work was rotating the key, checking whether it had been used, and auditing every place it had been valid — because the token lived on in the Git history and had to be assumed compromised. The lasting fix was a one-line habit: a `.gitignore` with `.env` in it, added to every new repo before the first commit. The whole class of incident disappeared.","visualAid":{"type":"comparison","title":"Commit it or ignore it?","headers":["File","Commit?","Why"],"rows":[["tests/, config, package.json","Commit","Source of truth CI needs"],["package-lock.json","Commit","Pins exact dependency versions"],["node_modules/","Ignore","Reinstalled from the lockfile"],[".env / tokens","Ignore","Secrets — a leak is permanent"],["playwright-report/, test-results/","Ignore","Generated on every run"]]},"davidTip":"The first commit in any new test repo should be the `.gitignore`, before you run `npm install`. It costs thirty seconds and it saves you from the two worst first-week mistakes on a shared repo: a tracked `node_modules` that will not stop conflicting, and a secret that quietly ends up in the history forever.","miniChallenge":"Write the `.gitignore` you would commit as the very first file in a new Playwright/Node repo. Include dependencies, Playwright output, and secrets — and be ready to say in one line why each entry is there."}$ench$::jsonb
from public.modules m join public.courses c on c.id = m.course_id
where l.module_id = m.id and c.slug = 'modern-test-automation-bootcamp'
  and m.module_number = 3 and l.lesson_number = 5;

