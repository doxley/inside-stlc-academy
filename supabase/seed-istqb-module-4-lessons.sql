-- ============================================================
-- API Testing Masterclass — Module 4 lessons (base fields).
-- Generated by scripts/content/build-module.mjs — do not hand-edit;
-- edit scripts/content/istqb-module4.mjs and regenerate. Run AFTER lessons-schema.sql,
-- add-api-testing-course.sql and seed-api-testing-modules.sql. Idempotent.
-- ============================================================

insert into public.lessons (module_id, lesson_number, title, estimated_time, lesson_overview, learning_objectives, lesson_notes, worked_example, common_mistakes, real_world_tip, exercise, reflection_question, knowledge_check, completion_checklist)
select m.id, 1, $L$Equivalence Partitioning$L$, $L$16 minute read$L$, $L$Equivalence Partitioning (EP) is the foundational black-box technique. It lets you reduce a huge number of possible inputs to a small, representative set of tests without losing coverage of the behaviour that matters.$L$, array[$L$Define equivalence partitioning and identify valid and invalid partitions$L$, $L$Derive test cases that give each partition one representative value$L$, $L$Explain the coverage measure for EP$L$], $L$## What it is
**Equivalence Partitioning** divides the input (or output) data of a component into **partitions** — groups the software should treat the same way. The theory: if one value in a partition reveals a defect, the others probably will too, and if one passes, the others probably pass too. So you test **one representative value per partition** rather than every value.

## Valid and invalid partitions
Every input has **valid** partitions (values the system should accept) and **invalid** partitions (values it should reject). ISTQB requires you to identify **both**. A common exam trap is deriving valid partitions only.

## A worked partition set
For an age field accepting **18–65**:

| Partition | Range | Type |
|---|---|---|
| P1 | below 18 (e.g. 10) | invalid |
| P2 | 18–65 (e.g. 30) | valid |
| P3 | above 65 (e.g. 70) | invalid |

## Coverage
**EP coverage = partitions exercised ÷ total partitions.** 100% coverage means every identified partition (valid and invalid) has at least one test. EP is usually the first pass; you then sharpen it with Boundary Value Analysis.$L$, $L$A cinema ticket price depends on age: child (0–15) £5, adult (16–64) £10, senior (65+) £8. Ages below 0 are invalid. Partitions: P1 invalid (< 0, e.g. -3); P2 valid child (0–15, e.g. 8); P3 valid adult (16–64, e.g. 40); P4 valid senior (65+, e.g. 70). Four partitions → four test cases, each with one representative value. Testing every age 0–120 would be 120+ cases for the same information.$L$, $L$- Deriving only valid partitions and forgetting the invalid ones (a classic exam trap)
- Testing several values from the same partition, which adds no coverage but inflates the test count
- Combining multiple invalid inputs in one test so you cannot tell which one caused the failure$L$, $L$On real projects EP is how you justify test count to a manager: "these 6 tests cover every distinct behaviour of the field" is far more defensible than "I tried a few numbers". It turns intuition into a coverage argument.$L$, $L$A password field accepts 8–20 characters. Identify all input partitions (valid and invalid) for length, then write one test case per partition with a representative value.$L$, $L$Why does testing two values from the same equivalence partition usually add no extra coverage?$L$, $L$A field accepts integers 1–100. Using equivalence partitioning on value alone, how many partitions exist and how many are invalid? (Answer: 3 partitions — one valid (1–100) and two invalid (< 1 and > 100))$L$, array[$L$I can identify valid and invalid partitions for a field$L$, $L$I can pick one representative value per partition$L$, $L$I can state the EP coverage measure$L$]
from public.modules m join public.courses c on c.id = m.course_id
where c.slug = 'istqb-foundation-masterclass' and m.module_number = 4
on conflict (module_id, lesson_number) do nothing;

insert into public.lessons (module_id, lesson_number, title, estimated_time, lesson_overview, learning_objectives, lesson_notes, worked_example, common_mistakes, real_world_tip, exercise, reflection_question, knowledge_check, completion_checklist)
select m.id, 2, $L$Boundary Value Analysis$L$, $L$17 minute read$L$, $L$Boundary Value Analysis (BVA) sharpens equivalence partitioning by testing the edges of partitions — where defects cluster most heavily. Getting the exact boundary values right is one of the most-tested skills in the ISTQB exam.$L$, array[$L$Explain why defects cluster at partition boundaries$L$, $L$Apply the 2-value and 3-value boundary approaches$L$, $L$Identify the correct boundary values for a given range$L$], $L$## Why boundaries
Programmers make off-by-one errors: `>` instead of `>=`, `<` instead of `<=`. These defects live exactly at the **edges** of partitions. BVA tests those edges deliberately. It is an extension of EP — you first partition, then test the boundaries between partitions.

## The 2-value approach
Test the boundary value and its nearest neighbour on the **other side** of the boundary. For a range **1–10**, the boundaries are 1 and 10; the 2-value set is **0, 1** and **10, 11**.

## The 3-value approach
Test the boundary and **both** neighbours. For 1–10: **0, 1, 2** and **9, 10, 11**. The 3-value approach is more thorough and is the one the current syllabus emphasises.

## Worked boundaries for 1–10

| Boundary | 2-value set | 3-value set |
|---|---|---|
| Lower (1) | 0, 1 | 0, 1, 2 |
| Upper (10) | 10, 11 | 9, 10, 11 |

## Coverage
**BVA coverage = boundary values tested ÷ total boundary values.** The min and max of the *valid* partition are themselves valid; the values just outside are invalid.$L$, $L$A form field accepts a quantity of **1 to 999**. Valid partition boundaries are 1 (lower) and 999 (upper). Using the **3-value approach**: lower boundary → 0, 1, 2; upper boundary → 998, 999, 1000. So the boundary tests are 0, 1, 2, 998, 999, 1000. Note 0 and 1000 are invalid (just outside), while 1, 2, 998, 999 are valid. A tester who wrote "test 1 and 999" would miss the off-by-one defect where the code used `> 999` instead of `>= 999`.$L$, $L$- Testing only the boundary itself and not the value just outside it — the neighbour is where off-by-one bugs show
- Confusing the 2-value and 3-value approaches under exam pressure
- Getting the wrong boundary: for a range "up to but not including 100", the boundary is 99/100, not 100/101$L$, $L$In real code reviews, the fastest way to find a bug is to ask "what happens at exactly the limit?" Free trials that end "after 30 days", discounts "over £100", rate limits "max 5 attempts" — the boundary is where the money and the defects are.$L$, $L$A bank allows withdrawals from £10 up to £500 per transaction. Using the 3-value boundary approach, list every boundary value you would test and mark each as valid or invalid.$L$, $L$Why is testing the value just *outside* a boundary as important as testing the boundary itself?$L$, $L$A field accepts values 1–100. Using the 2-value boundary approach, how many boundary values are there and what are they? (Answer: 4 values — 0, 1, 100, 101)$L$, array[$L$I can explain why defects cluster at boundaries$L$, $L$I can apply both the 2-value and 3-value approaches$L$, $L$I can identify correct boundary values for a stated range$L$]
from public.modules m join public.courses c on c.id = m.course_id
where c.slug = 'istqb-foundation-masterclass' and m.module_number = 4
on conflict (module_id, lesson_number) do nothing;

insert into public.lessons (module_id, lesson_number, title, estimated_time, lesson_overview, learning_objectives, lesson_notes, worked_example, common_mistakes, real_world_tip, exercise, reflection_question, knowledge_check, completion_checklist)
select m.id, 3, $L$Decision Table Testing$L$, $L$18 minute read$L$, $L$Decision tables handle the situation EP and BVA cannot: business rules where the outcome depends on a combination of conditions. They give you a systematic way to test complex logic without missing a rule.$L$, array[$L$Build a decision table from conditions and actions$L$, $L$Distinguish full coverage from collapsed/minimised tables$L$, $L$Derive one test case per rule (column)$L$], $L$## What it is
A **decision table** captures a set of **conditions** and the **actions** the system should take for each combination of those conditions. Each **column is a rule**; each rule becomes at least one test case. It excels where requirements read "if A and B but not C, then…".

## Structure
The table has a **condition** section (the inputs) and an **action** section (the outcomes). Conditions are usually T/F. For **n** binary conditions there are **2ⁿ** possible combinations (rules).

## A worked table
Insurance discount: give a discount if the customer is a **member** AND has **no claims** this year.

| | R1 | R2 | R3 | R4 |
|---|---|---|---|---|
| Member? | T | T | F | F |
| No claims? | T | F | T | F |
| **Action: discount** | Yes | No | No | No |

Two conditions → 2² = 4 rules → 4 test cases.

## Coverage
- **Minimum (full) coverage** = one test case per **rule (column)** = 100% decision table coverage.
- **Collapsed tables** merge columns where a condition is irrelevant using a dash (**—**, "don't care"), reducing the number of tests while keeping coverage of the distinct outcomes.$L$, $L$A login system: access is granted only if the account is **active**, the **password is correct**, and the account is **not locked**. Three binary conditions → 2³ = 8 rules. In a full table, only the rule (Active=T, Password OK=T, Locked=F) grants access; the other seven deny it. Because "locked" alone always denies access regardless of the other two, you can *collapse* the locked=T rules into fewer columns using "—" for the don't-care conditions — turning 8 columns into as few as 4 while still testing every distinct outcome.$L$, $L$- Forgetting a condition combination, so a whole business rule goes untested
- Miscounting the rules — n binary conditions give 2ⁿ columns, not 2×n
- Collapsing columns incorrectly and losing a genuinely different outcome$L$, $L$Decision tables are the technique that most impresses business analysts and product owners. When a rule set is tangled, drawing the table live in a refinement session often reveals combinations the BA never specified — you find requirement gaps, not just test cases.$L$, $L$A loan is approved only if the applicant is employed AND has a credit score above 600 AND is not already in arrears. Build the full decision table (conditions, rules, action) and state how many test cases full coverage requires.$L$, $L$Why does collapsing a decision table with "don't care" entries reduce test cases without reducing coverage of outcomes?$L$, $L$A decision table has 4 independent binary conditions. How many rules (columns) does the full, non-collapsed table contain? (Answer: 2⁴ = 16 rules)$L$, array[$L$I can build a decision table from conditions and actions$L$, $L$I can count the rules for n binary conditions$L$, $L$I can derive one test case per rule$L$]
from public.modules m join public.courses c on c.id = m.course_id
where c.slug = 'istqb-foundation-masterclass' and m.module_number = 4
on conflict (module_id, lesson_number) do nothing;

insert into public.lessons (module_id, lesson_number, title, estimated_time, lesson_overview, learning_objectives, lesson_notes, worked_example, common_mistakes, real_world_tip, exercise, reflection_question, knowledge_check, completion_checklist)
select m.id, 4, $L$State Transition Testing$L$, $L$17 minute read$L$, $L$Some systems behave differently depending on what happened before — the same input produces a different result in a different state. State transition testing models this behaviour and tests the moves between states, including the ones that should be impossible.$L$, array[$L$Read and build a state transition diagram or table$L$, $L$Identify valid transitions and invalid (should-not-happen) transitions$L$, $L$Distinguish 0-switch and 1-switch coverage$L$], $L$## What it is
A **state transition** model has **states** (the condition the system is in), **events** (inputs that trigger change), **transitions** (moves from one state to another caused by an event), and optional **actions/outputs**. It fits anything with memory: order lifecycles, media players, ATMs, authentication lockouts.

## Diagram vs table
A **state transition diagram** shows it visually; a **state transition table** lists every state–event pair. The table's power is that it forces you to consider events that should do **nothing or be rejected** in a given state — the invalid transitions a diagram often hides.

## A worked model (a door)
States: Closed, Open, Locked. Events: open, close, lock, unlock.

| Current state | Event | Next state |
|---|---|---|
| Closed | open | Open |
| Closed | lock | Locked |
| Open | close | Closed |
| Locked | unlock | Closed |
| Locked | open | (invalid — no transition) |

## Coverage
- **0-switch (all transitions)** = test every single valid transition once.
- **1-switch** = test every valid pair of consecutive transitions — more thorough, catches defects that depend on the previous move.
- Strong testing also fires **invalid** events in each state to confirm the system rejects them.$L$, $L$A phone PIN lock: state **Unlocked**; wrong PIN moves toward lockout. States: Unlocked, Attempt-1-Failed, Attempt-2-Failed, Locked. Event "wrong PIN" transitions Unlocked → Attempt-1-Failed → Attempt-2-Failed → Locked (after 3 failures). Event "correct PIN" from any failed state → Unlocked. From **Locked**, a correct PIN should do nothing (invalid transition) until a timeout. 0-switch coverage tests each arrow once; a strong test also fires "correct PIN" while Locked to confirm it is refused — a rule teams frequently get wrong.$L$, $L$- Testing only the "happy path" transitions and never firing invalid events in a state
- Confusing a state (a condition the system rests in) with an event (the trigger)
- Assuming 0-switch coverage is enough when the defect depends on the previous transition (needs 1-switch)$L$, $L$The most valuable state-transition tests are the invalid ones — "what happens if I pay for an order that's already cancelled?" Real production incidents are overwhelmingly about a system accepting an event it should have rejected because it was in the wrong state.$L$, $L$Model a traffic light with states Red, Green, Amber and the timer event that advances it. Draw the state transition table, then identify one invalid transition and describe how you would test that the system rejects it.$L$, $L$What kind of defect can 1-switch coverage find that 0-switch coverage cannot?$L$, $L$In state transition testing, what is the difference between a state and an event? (Answer: a state is the condition the system is currently in; an event is the input/trigger that may cause a transition to another state)$L$, array[$L$I can build a state transition table from a description$L$, $L$I can identify valid and invalid transitions$L$, $L$I can explain 0-switch vs 1-switch coverage$L$]
from public.modules m join public.courses c on c.id = m.course_id
where c.slug = 'istqb-foundation-masterclass' and m.module_number = 4
on conflict (module_id, lesson_number) do nothing;

insert into public.lessons (module_id, lesson_number, title, estimated_time, lesson_overview, learning_objectives, lesson_notes, worked_example, common_mistakes, real_world_tip, exercise, reflection_question, knowledge_check, completion_checklist)
select m.id, 5, $L$Pairwise Testing$L$, $L$16 minute read$L$, $L$When a feature has many parameters, testing every combination explodes into thousands of cases. Pairwise testing (a combinatorial technique) gives you a fraction of the tests while still covering every pair of values — where most combination defects actually live.$L$, array[$L$Explain why combinatorial explosion makes all-combinations testing impractical$L$, $L$Describe how pairwise testing covers all pairs of parameter values$L$, $L$Apply pairwise thinking to reduce a large combination space$L$], $L$## The problem it solves
Suppose a checkout has: payment (3 options), delivery (3 options), currency (4 options), device (3 options). All combinations = 3 × 3 × 4 × 3 = **108 tests**. Add one more parameter and it balloons further. Testing all combinations is rarely feasible.

## The insight
Research shows most combination defects are triggered by the interaction of just **two** parameters, not four or five at once. **Pairwise (2-wise) testing** selects a small set of tests such that **every pair of values from any two parameters appears together at least once**.

## The payoff
For the 108-combination example above, a pairwise set typically needs around **12 tests** — roughly a 90% reduction — while still covering every value-pair. Tools (PICT, ACTS, online generators) build the set for you; the skill is knowing *when* it is the right technique.

## A tiny worked case
Two parameters, each with 2 values (Browser: Chrome/Firefox; OS: Win/Mac). All combinations = 4, and pairwise also needs 4 here — pairwise only saves you when parameters and values grow. Its power appears with three or more parameters.$L$, $L$A report generator has: format (PDF, CSV, HTML), range (day, week, month), and audience (internal, external). All combinations = 3 × 3 × 2 = 18 tests. A pairwise set covering every pair of values needs about **9 tests** — for example (PDF, day, internal), (PDF, week, external), (PDF, month, internal), (CSV, day, external), (CSV, week, internal), (CSV, month, external), (HTML, day, internal), (HTML, week, external), (HTML, month, external). Check any two columns and every value-pair appears — half the tests, full 2-wise coverage.$L$, $L$- Using pairwise when a specific 3-way combination is known to be risky — pairwise guarantees pairs, not triples
- Assuming pairwise finds *all* defects; it targets 2-parameter interactions, not single-value or higher-order faults
- Reaching for pairwise on a feature with only one or two parameters, where it saves nothing$L$, $L$Pairwise is the technique that saves a compatibility or configuration test matrix from being impossible. When a QA lead says "we can't test every browser × OS × locale × plan combination", pairwise is the honest, defensible answer that still covers the interactions where bugs hide.$L$, $L$A form has three dropdowns: Country (UK, US, DE), Language (EN, DE), and Plan (Free, Pro). List all combinations, count them, then sketch a pairwise set and count how many tests it needs.$L$, $L$Why does pairwise testing catch most combination defects even though it does not test every possible combination?$L$, $L$A feature has 4 parameters with 3, 3, 2 and 2 values. How many tests does exhaustive all-combinations testing require? (Answer: 3 × 3 × 2 × 2 = 36 tests — pairwise would need far fewer)$L$, array[$L$I can explain combinatorial explosion$L$, $L$I can describe what "all pairs covered" means$L$, $L$I know when pairwise is and is not the right choice$L$]
from public.modules m join public.courses c on c.id = m.course_id
where c.slug = 'istqb-foundation-masterclass' and m.module_number = 4
on conflict (module_id, lesson_number) do nothing;

insert into public.lessons (module_id, lesson_number, title, estimated_time, lesson_overview, learning_objectives, lesson_notes, worked_example, common_mistakes, real_world_tip, exercise, reflection_question, knowledge_check, completion_checklist)
select m.id, 6, $L$Use Case Testing$L$, $L$15 minute read$L$, $L$Use case testing derives tests from how an actor actually interacts with the system to achieve a goal. It is the technique that best captures end-to-end business flows and the alternative paths real users take.$L$, array[$L$Explain what a use case is and its main/alternative/exception flows$L$, $L$Derive test cases from the basic flow and each alternative flow$L$, $L$Distinguish use case testing from the lower-level black-box techniques$L$], $L$## What it is
A **use case** describes an interaction between an **actor** (a user or another system) and the system to achieve a goal. Use case testing derives test cases from these interactions, and is especially good at finding **integration** and **end-to-end** defects — the ones that only appear when steps are joined up.

## Flows
- **Basic (main) flow** — the "everything goes right" path to the goal.
- **Alternative flows** — valid variations that still reach the goal (e.g. paying by a different method).
- **Exception flows** — error paths where the goal cannot be met (e.g. payment declined).

## Deriving tests
At minimum, one test case for the basic flow and **one per alternative and exception flow**. Each flow through the use case is a test.

## A worked use case
"Withdraw cash from an ATM":

| Flow | Description | Test outcome |
|---|---|---|
| Basic | Valid card, correct PIN, sufficient funds | Cash dispensed |
| Alternative | Correct PIN, chooses receipt | Cash + receipt |
| Exception | Insufficient funds | Declined, no cash |
| Exception | Wrong PIN ×3 | Card retained |$L$, $L$Use case "Place an online order". Basic flow: browse → add to basket → checkout → pay → order confirmed. Alternative flow: apply a valid discount code before paying (still reaches "order confirmed"). Exception flows: card declined (order not placed, user prompted to retry); item goes out of stock during checkout (basket updated, user warned). From this one use case you derive at least four test cases — the basic path plus each alternative and exception — covering the journey the way a real customer travels it, not just isolated fields.$L$, $L$- Testing only the basic flow and skipping the alternative and exception flows where most real defects live
- Confusing use case testing (behavioural, end-to-end) with use case *diagrams* (just notation)
- Writing use cases so vague that no concrete test steps can be derived from them$L$, $L$Use case testing is your bridge to acceptance testing and to the business. Because each flow maps to something a stakeholder recognises ("what if the card is declined?"), use-case-derived tests are the easiest to review with product owners and the most convincing evidence that the system does what users need.$L$, $L$Write a use case for "Reset a forgotten password". Identify the basic flow, at least one alternative flow, and at least one exception flow, then derive a test case for each.$L$, $L$Why do the alternative and exception flows of a use case tend to reveal more defects than the basic flow?$L$, $L$In use case testing, what are the three kinds of flow you derive test cases from? (Answer: the basic/main flow, alternative flows, and exception flows)$L$, array[$L$I can identify basic, alternative and exception flows$L$, $L$I can derive a test case from each flow$L$, $L$I can explain how use case testing differs from EP/BVA$L$]
from public.modules m join public.courses c on c.id = m.course_id
where c.slug = 'istqb-foundation-masterclass' and m.module_number = 4
on conflict (module_id, lesson_number) do nothing;

insert into public.lessons (module_id, lesson_number, title, estimated_time, lesson_overview, learning_objectives, lesson_notes, worked_example, common_mistakes, real_world_tip, exercise, reflection_question, knowledge_check, completion_checklist)
select m.id, 7, $L$Experience-Based Testing$L$, $L$16 minute read$L$, $L$Not all testing comes from a formal model. Experience-based techniques — error guessing, exploratory testing, and checklist-based testing — draw on a tester's knowledge, intuition and experience to find defects the systematic techniques miss.$L$, array[$L$Define error guessing, exploratory testing, and checklist-based testing$L$, $L$Explain how experience-based techniques complement black-box techniques$L$, $L$Recognise when experience-based testing adds the most value$L$], $L$## Where they fit
The techniques so far (EP, BVA, decision tables, state transition, use cases) are **black-box** — derived from a specification. **White-box** techniques (statement and branch/decision coverage) instead derive tests from the code's internal structure — mentioned here only for context, as they belong to a separate part of the syllabus. **Experience-based** techniques are the third family: tests come from the **tester's** knowledge, not a model.

## The three techniques
- **Error guessing** — deliberately anticipating likely mistakes (empty inputs, zero, negative numbers, duplicate submits, special characters) based on where defects usually hide. Often formalised as a **fault attack** list.
- **Exploratory testing** — simultaneous learning, test design and execution, usually **time-boxed** into sessions with a **charter** (a mission for the session). Structured exploratory work is often called **session-based test management**.
- **Checklist-based testing** — testing guided by a checklist of items to verify (e.g. accessibility, security, usability heuristics), capturing lessons and standards in reusable form.

## Their role
Experience-based techniques **complement**, not replace, the systematic ones. Their coverage is hard to measure and depends heavily on the tester, but they excel where specifications are weak, missing, or where subtle usability and edge-case defects live.$L$, $L$A team has just applied EP and BVA to a new registration form and everything passes. A tester then runs a 60-minute **exploratory session** with the charter "explore the registration form for input-handling defects". Using **error guessing**, they try: pasting a 5,000-character name, submitting twice rapidly, entering emoji in the name field, and an email with a trailing space. The double-submit creates two accounts — a concurrency defect no partition or boundary described, because it exists in the *behaviour*, not the *input space*. Systematic techniques found the specified defects; experience-based testing found the one nobody specified.$L$, $L$- Treating exploratory testing as "random clicking" — real exploratory testing is chartered, focused and documented
- Relying on experience-based testing *instead of* systematic techniques rather than alongside them
- Assuming its coverage can be measured the way EP or BVA coverage can — it cannot, and that is expected$L$, $L$The best testers pair a systematic pass with a time-boxed exploratory session on every feature. The systematic pass proves the specified behaviour; the exploratory session — where your experience runs free — finds the defects the specification never imagined. Charters keep it focused enough to be credible to a manager.$L$, $L$Pick any app you use. Write a 30-minute exploratory testing charter for one feature (state the mission), then list five error-guessing inputs you would try and why each is likely to expose a defect.$L$, $L$Why can experience-based testing find defects that equivalence partitioning and boundary value analysis structurally cannot?$L$, $L$Name the three experience-based test techniques in the ISTQB Foundation syllabus. (Answer: error guessing, exploratory testing, and checklist-based testing)$L$, array[$L$I can define error guessing, exploratory and checklist-based testing$L$, $L$I can explain how they complement black-box techniques$L$, $L$I can write a focused exploratory charter$L$]
from public.modules m join public.courses c on c.id = m.course_id
where c.slug = 'istqb-foundation-masterclass' and m.module_number = 4
on conflict (module_id, lesson_number) do nothing;

insert into public.lessons (module_id, lesson_number, title, estimated_time, lesson_overview, learning_objectives, lesson_notes, worked_example, common_mistakes, real_world_tip, exercise, reflection_question, knowledge_check, completion_checklist)
select m.id, 8, $L$Selecting the Right Technique$L$, $L$18 minute read$L$, $L$Knowing the techniques is half the skill; choosing the right one for the situation is the other half. This capstone lesson shows how to match technique to context and combine techniques into a coherent test design — the mark of a professional tester.$L$, array[$L$Match each test design technique to the situations where it is strongest$L$, $L$Combine complementary techniques into a layered test design$L$, $L$Justify a technique choice in terms of risk, coverage and effort$L$], $L$## The core idea
No single technique is "best". Each targets a different kind of defect. Skilled testers **layer** techniques: partition the input space, sharpen the edges, cover the rule combinations, walk the states, tame the parameter explosion, follow the user journey, then explore.

## Quick selection guide

| If the feature is about… | Reach for… |
|---|---|
| A field with ranges/categories | Equivalence Partitioning |
| Limits, edges, off-by-one risk | Boundary Value Analysis |
| Combinations of business rules | Decision Table Testing |
| Behaviour that depends on history/status | State Transition Testing |
| Many independent parameters | Pairwise Testing |
| End-to-end user goals & flows | Use Case Testing |
| Weak/missing specs, subtle bugs | Experience-Based Testing |

## Combining, not choosing
On one checkout form you might use **EP** on the promo-code field, **BVA** on the quantity, a **decision table** for the discount rules, **state transition** for the order lifecycle, **pairwise** for payment × delivery × currency, a **use case** for the full journey, and an **exploratory** session to finish. Selection is really *composition*.

## Factors that drive the choice
Risk level, contractual/regulatory requirements, defect history, the quality of the specification, and the time and skills available all steer which techniques earn their place.$L$, $L$Brief: test a "book a flight" feature. Layered design: **EP/BVA** on passenger count (1–9) and date ranges; **decision table** for the fare rules (member × baggage × flexible ticket → price and change-fee); **state transition** for the booking lifecycle (searching → held → paid → ticketed → cancelled, including invalid moves like paying a cancelled hold); **pairwise** for cabin × meal × seat-type × payment combinations; a **use case** per journey (basic booking, booking with a voucher, payment-declined exception); and a time-boxed **exploratory** session on the seat-map UI. One feature, seven techniques, each covering a defect class the others cannot — and a coverage story you can defend to any manager or auditor.$L$, $L$- Believing one technique fits everything, and forcing (say) BVA onto a problem that is really a decision table
- Choosing techniques by habit rather than by the risk and nature of the feature
- Over-testing low-risk areas with heavy techniques while under-testing high-risk logic$L$, $L$In interviews and appraisals, the question that separates senior testers is "how would you test this?" The weak answer names one technique; the strong answer *layers* several and *justifies each by the risk it addresses*. That reasoning — technique matched to risk — is the whole point of Module 4.$L$, $L$Take a feature you know well (e.g. an online checkout, a booking form, or a settings page). Design a layered test approach naming at least four techniques from this module, and for each write one sentence justifying why it fits that part of the feature.$L$, $L$Why is combining several techniques on one feature usually stronger than picking a single "best" technique?$L$, $L$Which technique is most appropriate when a system's response to an event depends on what state it is currently in? (Answer: state transition testing)$L$, array[$L$I can match a technique to the kind of defect it targets$L$, $L$I can combine techniques into a layered design for one feature$L$, $L$I can justify each technique choice in terms of risk and coverage$L$]
from public.modules m join public.courses c on c.id = m.course_id
where c.slug = 'istqb-foundation-masterclass' and m.module_number = 4
on conflict (module_id, lesson_number) do nothing;

